// script.js
function generateJSONResponse() {
    // Generate or fetch the JSON data
    var jsonData = { redrct:true };
  
    // Return the JSON response
    return JSON.stringify(jsonData);
  }
  